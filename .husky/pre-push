cd services/retroboard-server

echo "Running server tests..."
npx vitest run
if [ $? -ne 0 ]; then
  echo ""
  echo "Server tests failed. Fix errors before pushing."
  exit 1
fi

echo "Running client tests..."
cd client && npx vitest run
if [ $? -ne 0 ]; then
  echo ""
  echo "Client tests failed. Fix errors before pushing."
  exit 1
fi
